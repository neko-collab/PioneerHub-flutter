name: pioneerhub_app
description: "A new Flutter project."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  khalti_flutter: ^3.0.0
  http: 
  provider: 
  json_annotation: 
  hive: 
  hive_flutter: 
  path_provider:
  file_picker: ^10.1.2
  intl: ^0.18.0
  url_launcher: ^6.3.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner:
  hive_generator:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/